<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM 'http://java.sun.com/dtd/properties.dtd'>
<!--
  Configuration Traccar — PRODUCTION.
  Base EMBARQUÉE H2 (fichier sur le volume `traccar-data`), isolée du métier — pas de
  couplage au Postgres métier. Format calqué sur le traccar.xml d'origine de l'image 6.14
  (les défauts viennent du jar, il n'y a plus de conf/default.xml). API REST = provisioning/commandes.
-->
<properties>

    <!-- Base embarquée H2 (driver bundlé dans l'image). -->
    <entry key='database.driver'>org.h2.Driver</entry>
    <entry key='database.url'>jdbc:h2:./data/database</entry>
    <entry key='database.user'>sa</entry>
    <entry key='database.password' />

    <!-- API Web (interne uniquement — non exposée par nginx). -->
    <entry key='web.port'>8082</entry>
    <entry key='web.origin'>*</entry>

</properties>
