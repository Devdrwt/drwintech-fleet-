export default function Home() {
  return (
    <main style={{ padding: 40, fontFamily: "system-ui" }}>
      <h1>Drwintech Fleet</h1>
      <p>Plateforme indépendante de gestion de flotte GPS.</p>
      <p>
        Squelette frontend — voir <code>docs/PLAN.md</code> pour la feuille de
        route. Démarrage : <code>npm install &amp;&amp; npm run dev</code>.
      </p>
      <ul>
        <li>
          <a href="/login">Connexion (à implémenter — Phase 2)</a>
        </li>
      </ul>
    </main>
  );
}
